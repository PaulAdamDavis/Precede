<nav>
    <a href="{{@blog.url}}">
        {{#if @blog.logo}}
            <img src="{{@blog.logo}}" alt="Blog Logo" width="200" />
        {{else}}
            {{@blog.title}}
        {{/if}}
    </a>
</nav>
{{navigation}}